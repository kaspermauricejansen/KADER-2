{
  "name": "studio-kader-website",
  "version": "1.0.0",
  "private": true,
  "description": "Statische website van Studio Kader — interieurfotografie en cinematische walkthroughs, Breda.",
  "scripts": {
    "build:css": "tailwindcss -i ./src/site.css -o ./assets/css/site.css --minify",
    "watch:css": "tailwindcss -i ./src/site.css -o ./assets/css/site.css --watch",
    "build:pages": "node ./tools/build.mjs",
    "build": "npm run build:pages && npm run build:css"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
